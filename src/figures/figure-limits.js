export const FIGURE_PIPELINE_PROFILE = 'figure-region-v1';

export const FIGURE_LIMITS = Object.freeze({
    maxPDFBytes: 256 * 1024 * 1024,
    maxPDFPages: 10_000,
    maxMarkdownBytes: 50 * 1024 * 1024,
    maxContentListBytes: 20 * 1024 * 1024,
    maxDetailedLayoutBytes: 20 * 1024 * 1024,
    maxCombinedLayoutBytes: 40 * 1024 * 1024,
    maxLayoutBlocks: 100_000,
    maxJSONNodes: 500_000,
    maxJSONDepth: 12,
    maxFiguresPerPage: 64,
    maxPanels: 256,
    maxFigures: 1_000,
    maxComparisons: 2_000_000,
    maxMapBytes: 8 * 1024 * 1024,
    maxCaptionLength: 8_192,
    maxFragmentLength: 65_536,
    maxCropPixels: 8_000_000,
    maxCropEdge: 4_096,
    maxDecodedImagePixels: 16_000_000,
    maxIntermediateCanvasPixels: 16_000_000,
    maxIntermediateCanvasEdge: 8_192,
    maxActiveCanvasPixels: 32_000_000,
    maxCropBytes: 12 * 1024 * 1024,
    maxGeneratedBytes: 64 * 1024 * 1024,
    maxTotalAssetBytes: 150 * 1024 * 1024,
    maxAssets: 1_000,
    maxInputAssets: 2_000,
    renderConcurrency: 1,
    cropTimeoutMs: 20_000,
    documentTimeoutMs: 120_000,
    defaultDpi: 192,
    padding: 2,
    geometryTolerance: 0.01,
    panelProjectionOverlap: 0.35,
    panelMinGap: 24,
    panelMaxGap: 180,
    panelGapFactor: 0.6,
    captionProjectionOverlap: 0.5,
    captionMinGap: 40,
    captionMaxGap: 120,
    captionGapFactor: 0.15,
    interiorTextRatio: 0.9,
    maxGapTextLines: 2,
    maxGapTextCodePoints: 120,
});

export const FIGURE_PRESERVE_REASONS = Object.freeze([
    'missing-geometry',
    'coordinate-mismatch',
    'ambiguous-caption',
    'ambiguous-membership',
    'ambiguous-source-range',
    'caption-overlap',
    'foreign-content-overlap',
    'unsupported-layout-schema',
    'resource-limit',
    'render-failed',
    'render-timeout',
]);

export function isFigurePreserveReason(value) {
    return FIGURE_PRESERVE_REASONS.includes(value);
}
